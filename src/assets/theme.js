import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#212121", // black
    },
    secondary: {
      main: "#880e4f", // deep pink
    },
  },
});

export default theme;
