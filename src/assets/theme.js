import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#310B0B", // dark brown
    },
    secondary: {
      main: "#E2703A", // orange ish
    },
  },
});

export default theme;
